import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.persian_ocr.application',
  appName: 'Persian OCR',
  webDir: 'dist'
};

export default config;
